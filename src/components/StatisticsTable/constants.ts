import { colorSchemeDarkWarm, themeQuartz } from "ag-grid-community";

export const THEME = themeQuartz.withPart(colorSchemeDarkWarm).withParams({
  backgroundColor: 'rgb(14, 13, 12)',
  rowHoverColor: '#261e16',
});